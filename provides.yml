client_tabs:
    wifi-tab:
        view: wifi_tab
        i18n: wifi.wifi
        badge: wifi-cnt
listings:
    wifi:
        view: wifi_listing
        i18n: wifi.wifi
reports:
    wifi_report:
        view: wifi_report
        i18n: wifi.report
widgets:
    wifi_networks:
        view: wifi_networks_widget
    wifi_security:
        view: wifi_security_widget
    wifi_state:
        view: wifi_state_widget
    wifi_snr:
        view: wifi_snr_widget
    wifi_bssid_aliases:
        view: wifi_bssid_aliases_widget

# Module unique name
slug: wifi

# Admin (1) or user (0) module
admin: 0

# Modules that this module is dependent on
dependencies:
    - reportdata